# FFtester
A quick software to compare/test different encoding settings in FFmpeg. Binaries comes with prebuilt integrated FFmpeg, else you can install FFmpeg system-wide or just in the same folder as the script.
